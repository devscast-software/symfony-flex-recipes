name: Update Flex endpoint

on:
    push:
        branches:
            - main

defaults:
    run:
        shell: bash

jobs:
    call-flex-update:
        uses: devscast-software/symfony-flex-recipes/.github/workflows/callable-flex-update.yml@main
        with:
            contrib: true